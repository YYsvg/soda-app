カテゴリの作成

<%= form_with model: @income_category, local: true do |f| %>
    <div>
      <label>カテゴリー名</label><%= f.text_field :name %>
    </div>
    <%= f.submit "作成する" %>
<% end %>


<div>
  <p>編集、削除は一覧ページから</p>
  <%= link_to "一覧へ", income_categories_path %>
  <%= link_to "トップページへ", homes_path %>
</div>